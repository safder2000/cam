import 'package:flutter/material.dart';

class ScreenGallary extends StatelessWidget {
  const ScreenGallary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
